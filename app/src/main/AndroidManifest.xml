<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.doordashapp"
    android:installLocation="auto">

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/icon"
        android:label="@string/app_name"
        android:roundIcon="@id/icon"
        android:supportsRtl="true"
        android:theme="@style/DoorDashApp.NoActionBar"
        android:usesCleartextTraffic="true">
        <activity android:name=".FoodDonationActivity"></activity>
        <activity android:name=".activities.SettingsActivity" />
        <activity android:name=".activities.PaymentActivity"></activity>
        <activity android:name=".activities.OrderDetailsOwnerActivity" />
        <activity android:name=".activities.ShopReviewsActivity" />
        <activity android:name=".activities.WriteReviewActivity" />
        <activity android:name=".activities.OrderDetailsUsersActivity" />
        <activity
            android:name=".activities.ShopDetailsActivity"
            android:windowSoftInputMode="stateHidden" /> <!-- Hide keyboard by default -->
        <activity android:name=".activities.EditFoodActivity" />
        <activity android:name=".activities.ProfileEditUserActivity" />
        <activity android:name=".activities.addFoodActivity" />
        <activity android:name=".activities.ProfileEditOwnerActivity" />
        <activity android:name=".activities.MainUserActivity" />
        <activity android:name=".activities.MainOwnerActivity" />
        <activity android:name=".activities.RegisterOwnerActivity" />
        <activity android:name=".activities.RegisterUserActivity" />
        <activity android:name=".activities.ForgotPasswordActivity" />
        <activity android:name=".activities.LoginActivity" />
        <activity android:name=".activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.theartofdev.edmodo.cropper.CropImageActivity"
            android:theme="@style/Base.Theme.AppCompat" />
    </application>

</manifest>